interface Props {
    chain: string;
}

export const Connect = ({ chain }: Props) => {
    return (
        <div>
            <h3>Connect {chain} wallet</h3>
            <div>
                <button></button>
            </div>
        </div>
    );
};
